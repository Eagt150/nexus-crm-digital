import { PageStub } from "@/components/PageStub";

export default function ClientesPage() {
  return (
    <PageStub
      title="Clientes"
      note="Lista de clientes con búsqueda en tiempo real — pendiente de implementar (MCP-31)."
    />
  );
}
