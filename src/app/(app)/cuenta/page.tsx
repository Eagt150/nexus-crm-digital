import { PageStub } from "@/components/PageStub";

export default function CuentaPage() {
  return (
    <PageStub
      title="Mi cuenta"
      note="Editar datos, cambiar contraseña y cerrar sesión — pendiente de implementar (MCP-73)."
    />
  );
}
